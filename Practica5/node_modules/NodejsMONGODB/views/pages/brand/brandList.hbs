{{> header}}
<h2>{{viewTitle}}</h2>
<table border="1">
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Descripción</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        {{#each list}}
        <tr>
            <td>{{this.name}}</td>
            <td>{{this.description}}</td>
            <td>
                <a href="/brand/{{this._id}}">Actualizar</a>
                <a href="/brand/delete/{{this._id}}"
                onclick="return confirm('Desea eliminar el registro?')">Eliminar</a>
            </td>
        </tr>
        {{/each}}
        </tbody>
</table>